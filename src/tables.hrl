-record(employee, {emp_no,
                   name,
                   salary,
                   sex,
                   phone,
                   room_no}).

-record(client,{
    client_id,
    client_secret,
    redirect_uri
}).

-record(user, {
    username,
    password
}).